# ticket-booking-engine

## Running locally

1. Install PostgreSQL and create a database named `ticket_booking_auth`
2. Set the following environment variable before running:
    - `DB_PASSWORD` — your PostgreSQL password

   In IntelliJ: Run → Edit Configurations → Environment Variables → add `DB_PASSWORD=your_password`